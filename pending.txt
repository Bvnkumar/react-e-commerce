1)Create sub folder with name as 'node-backend';
2)Try to add move all react files into website folder then try npm start
3)Create a mongodb connection in app.js, it should be global visible, so that we can access it other touters as well.
4)At first, we just add the signup and login funcationalites with jwt token.
